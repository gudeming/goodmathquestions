{
  "name": "@gmq/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "set -a; . ../../.env; set +a; prisma generate",
    "db:push": "set -a; . ../../.env; set +a; prisma db push",
    "db:seed": "set -a; . ../../.env; set +a; tsx prisma/seed.ts",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
